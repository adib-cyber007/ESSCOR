version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: esscor_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-esscor}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-esscor_pass}
      POSTGRES_DB: ${POSTGRES_DB:-esscor_db}
    ports:
      - "5432:5432"
    volumes:
      - esscor_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-esscor}"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: esscor_pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@esscor.local
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - esscor_pgadmin:/var/lib/pgadmin

volumes:
  esscor_pgdata:
  esscor_pgadmin:
